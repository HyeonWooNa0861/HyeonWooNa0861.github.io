---
layout: default
title: "LS13 기본 정렬 알고리즘"
course: "자료구조"
topic: "기본 정렬 알고리즘"
order: 13
---

# LS13 기본 정렬 알고리즘 요약

원본 자료: `LS13_sorting.pdf`

## 전체 흐름

| 순서 | 주제 | 핵심 질문 |
|---|---|---|
| 1 | 정렬의 필요성 | 왜 데이터를 순서화해야 하는가? |
| 2 | 정렬 비용 기준 | 비교 횟수와 swap 횟수는 왜 중요한가? |
| 3 | 버블 정렬 | 큰 값을 오른쪽으로 보내는 방식은 어떻게 동작하는가? |
| 4 | 선택 정렬 | 매번 최솟값을 선택하면 어떤 비용이 드는가? |
| 5 | 삽입 정렬 | 정렬된 앞부분에 끼워 넣는 방식은 언제 유리한가? |

## 1. 정렬이 필요한 이유

정렬은 리스트의 원소를 특정 기준에 따라 순서화하는 작업이다.

정렬을 해 두면 다음 작업이 쉬워진다.

| 목적 | 설명 |
|---|---|
| 탐색 성능 향상 | 이진 탐색 같은 빠른 탐색을 적용할 수 있다. |
| 데이터 분석 | 중앙값, 분위수, 이상치 탐지가 쉬워진다. |
| 사용자 경험 | 가격순, 인기순 정렬처럼 원하는 정보를 빨리 찾게 한다. |
| 다른 알고리즘의 기반 | 많은 알고리즘이 정렬된 입력을 전제로 한다. |

## 2. 정렬 알고리즘의 비용 기준

정렬에서는 보통 두 비용을 본다.

| 비용 | 의미 |
|---|---|
| 비교 횟수 | 두 원소의 대소를 비교한 횟수 |
| 교환 횟수 | 두 원소의 위치를 바꾼 횟수 |

비교 횟수는 전체 시간 복잡도에 직접 연결되고, swap은 실제 데이터 이동 비용과 연결된다.

## 3. 버블 정렬 Bubble Sort

버블 정렬은 인접한 두 원소를 비교해 큰 값을 오른쪽으로 보낸다. 한 회전이 끝나면 가장 큰 값이 맨 오른쪽에 확정된다.

```java
void bubbleSort(List<Integer> list) {
    for (int i = 0; i < list.size() - 1; i++) {
        for (int j = 0; j < list.size() - 1 - i; j++) {
            if (list.get(j) > list.get(j + 1)) {
                swap(list, j, j + 1);
            }
        }
    }
}
```

| 항목 | 내용 |
|---|---|
| 전략 | 큰 값을 오른쪽 끝으로 밀어낸다. |
| 비교 횟수 | 대략 `n^2 / 2` |
| swap 횟수 | 최선 0, 최악 대략 `n^2 / 2` |
| 시간 복잡도 | `O(n^2)` |

버블 정렬은 직관적이지만 swap이 많이 발생할 수 있어 효율적이지 않다.

## 4. 선택 정렬 Selection Sort

선택 정렬은 정렬되지 않은 구간에서 가장 작은 값을 찾아 맨 앞 원소와 교환한다.

```java
void selectionSort(List<Integer> list) {
    int n = list.size();
    for (int i = 0; i < n - 1; i++) {
        int minIndex = i;
        for (int j = i + 1; j < n; j++) {
            if (list.get(j) < list.get(minIndex))
                minIndex = j;
        }
        swap(list, i, minIndex);
    }
}
```

| 항목 | 내용 |
|---|---|
| 전략 | 가장 작은 값을 왼쪽부터 확정한다. |
| 비교 횟수 | 대략 `n^2 / 2` |
| swap 횟수 | 최선 0, 최악 `n - 1` 수준 |
| 시간 복잡도 | `O(n^2)` |

선택 정렬은 비교는 많지만 swap 횟수가 적다. 데이터 이동 비용이 큰 상황에서는 버블 정렬보다 나을 수 있다.

## 5. 삽입 정렬 Insertion Sort

삽입 정렬은 왼쪽에 이미 정렬된 구간을 유지하고, 현재 원소를 그 구간의 적절한 위치에 끼워 넣는다.

```java
void insertionSort(int[] list) {
    for (int i = 1; i < list.length; i++) {
        for (int j = i; j > 0 && list[j] < list[j - 1]; j--) {
            int temp = list[j];
            list[j] = list[j - 1];
            list[j - 1] = temp;
        }
    }
}
```

| 항목 | 내용 |
|---|---|
| 전략 | 현재 원소를 앞쪽 정렬 구간에 삽입한다. |
| 비교 횟수 | 최선 `n - 1`, 최악 대략 `n^2 / 2` |
| swap 횟수 | 최선 0, 최악 대략 `n^2 / 2` |
| 시간 복잡도 | 최악 `O(n^2)` |

삽입 정렬은 거의 정렬된 리스트에서 매우 효과적이다. 이미 순서가 맞으면 내부 반복문이 거의 실행되지 않는다.

## 6. 세 기본 정렬 비교

| 알고리즘 | 전략 | 시간 복잡도 | 특징 |
|---|---|---|---|
| Bubble Sort | 큰 값을 뒤로 이동 | `O(n^2)` | swap이 많다. |
| Selection Sort | 작은 값을 앞으로 이동 | `O(n^2)` | swap이 적다. |
| Insertion Sort | 정렬 구간에 삽입 | `O(n^2)` | 거의 정렬된 입력에 강하다. |

## 시험 포인트

| 질문 | 답의 방향 |
|---|---|
| 버블 정렬 한 회전 후 확정되는 값은? | 현재 구간의 최댓값 |
| 선택 정렬의 swap 횟수가 적은 이유는? | 한 회전당 최대 한 번만 교환 |
| 삽입 정렬의 최선 시간은 왜 빠른가? | 이미 정렬되어 있으면 내부 이동이 거의 없음 |
| 세 알고리즘의 최악 시간 복잡도는? | 모두 `O(n^2)` |

## 복습 질문

1. `[7, 4, 5, 1, 3]`에서 버블 정렬 1회전 후 배열은 어떻게 되는가?
2. 선택 정렬과 삽입 정렬 중 거의 정렬된 배열에 더 적합한 것은?
3. 비교 횟수와 swap 횟수는 항상 같은가?
## PDF

<ul>
  <li><a href="{{ "/assets/pdfs/study/data-structure/LS13_sorting.pdf" | relative_url }}" target="_blank" rel="noopener">LS13_sorting.pdf</a></li>
</ul>
